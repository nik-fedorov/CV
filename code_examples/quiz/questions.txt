Что получится в результате выполнения выражения list(map(type, [1, 'a', (2,), 3.14])) ?
1. [int, str, int, float]|2. (int, str, tuple, float)|3. [int, str, tuple, float]|4. [int, str, list, int]
3
Перечислите все корректные названия переменных в питоне из списка ниже.
1. _|2. abra-cadabra|3. Abracadabra|4. 1person|5. this is my variable
1|3
Выберите из списка все изменяемые типы данных.
1. str|2. list|3. dict|4. float|5. set|6. bool
2|3|5
Что вернет это выражение float("3,14") ?
1. 3,14|2. 3.14|3. Код завершится с ошибкой
3
Что вернет следующее выражение 'Hello world!'[1] ?
1. 'H'|2. 'e'|3. Код завершится с ошибкой
2
Что произойдет при запуске следующего кода a=[];b=a;b+=[1];print(len(a))
1. Напечатается число 1|2. Напечатается число 0|3. Код завершится с ошибкой
1
У нас в коде встретились два вложенных цикла for: один for (внутренний) в теле другого for (внешнего). Что будет, если выполнить break в теле внутреннего цикла for ?
1. Остановится выполнение обоих циклов for|2. Остановится выполнение лишь внутреннего цикла for (внешний продолжит выполняться)
2
Цикл while 1==1 будет выполняться вечно, пока программу не прервут, либо пока изнутри цикла не вызовут break. Это утверждение ...
1. Верно|2. Неверно
1
Для каких чисел i из списка list(range(3, -10, -3)) следующее выражение выкинет исключение 0 / i ?
1. Для числа 0|2. Ни для каких|3. Для всех
1
Что вернет следующее выражение [0, -1, 1, 1, 1, 24].index(1) ?
1. 1|2. 2|3. 3|4. 4
2
Что вернет следующее выражение [int(x) for x in ['1', 2, 3.14, "2.71"]] ?
1. [1, 2, 3, 2]|2. Выражение завершится с ошибкой
2
Что вернет выражение ' Hello  world! '.split(' ') ?
1. ['', 'Hello', '', 'world!', '']|2. ['Hello', '', 'world!']|3. ['Hello', 'world!']
1
Корректно ли следующее определение словаря d = {'a': 2, 2: 'a', [1]: 1} ?
1. Да|2. Нет
2
Выберите все выражения, возвращающие True.
1. 1 in {1, 2}|2. 1 in {1: 2}|3. 2 in {1: 2}|4. type({}) == dict|5. len({'aaa': ['a', 'a', 'a']}) == 1
1|2|4|5
Какая из следующих записей корректно импортирует функцию sqrt модуля math?
1. from math import sqrt|2. import sqrt from math|3. import math from sqrt
1
Выберите все корректные заголовки функций из списка ниже.
1. f():|2. def f():|3. def func(a, b=3):|4. def f(*args):|5. def f(***args):
2|3|4
Верно ли, что при использовании with open(filename) as f явно закрывать файл (f.close()) НЕ нужно?
1. Верно|2. Это ложь, файлы всегда нужно закрывать!
1
Верно ли, что функции pandas.read_csv(filename) в качестве filename можно передавать не только путь к файлу в системе компьютера, но и ссылку на файлы, хранящиеся в интернете?
1. Верно|2. Неверно
1